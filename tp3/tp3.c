/*Écrire un programme qui demande deux nombres à 
l'utilisateur et affiche leur produit. */

int main() {
    int a, b;
    printf("Entrez deux nombres: ");
    scanf("%d %d", &a, &b);
    printf("Le produit est: %d\n", a * b);
    return 0;
}